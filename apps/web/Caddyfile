{$ELMS_DOMAIN} {
  tls {$ACME_EMAIL}
  encode zstd gzip

  reverse_proxy web:80

  log {
    output file /var/log/caddy/access.json {
      roll_size 10MiB
      roll_keep 5
    }
    format json
  }
}
